{
  "variables": {
    "ami_name": "",
    "aws_profile": "",
    "aws_region": "",
    "tag_name": ""
  },
  "builders": [{
    "ami_name": "{{user `ami_name`}}",
    "instance_type": "t2.medium",
    "profile": "{{user `aws_profile`}}",
    "region": "{{user `aws_region`}}",
    "source_ami": "ami-0240506e88917a482",
    "ssh_username": "ec2-user",
    "tags": {
       "Name": "{{user `tag_name`}}"
    },
    "type": "amazon-ebs"
  }],
  "provisioners": [
    {
      "inline": [
        "sudo dnf -y update"
      ],
      "type": "shell"
    }
  ]
}
